# cookinc source config (Windows)
sink:
  url: "http://vps-tailnet-ip:9876"
chrome:
  db_path: "%LOCALAPPDATA%\\Google\\Chrome\\User Data\\Default\\Network\\Cookies"
security:
  shared_secret: "replace-with-32-char-min-secret"
allowlist:
  domains:
    - "github.com"
    - "vercel.com"
    - "x.com"
    - "gitlab.com"
watch:
  interval: "5s"
